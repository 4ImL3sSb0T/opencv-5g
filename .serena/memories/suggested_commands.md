Build: `cmake -S . -B build` then `cmake --build build`. Run executables from build/: `opencv_5g.exe`, `guided.exe`, `trace.exe`, `line_detection.exe`, `demo.exe`. ONNX check: `onnx_test.exe model/LR.onnx img/test.jpg --size 320x320`. Module tests: `arrow_test.exe`, `ab_test.exe`, `test_yolo_infer.exe`. Set `OpenCV_DIR`/`spdlog_DIR` if missing; ONNX runtime in lib/onnxruntime-win-x64-1.23.2. Windows PowerShell basics: `Get-ChildItem`, `cmake --build build --target <tgt>`, run `.uild\<exe>.exe`. No formal lint/format; validate via provided executables with sample media.