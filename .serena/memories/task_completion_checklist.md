After changes: rebuild with `cmake --build build` (configure first with `cmake -S . -B build` if needed). Run relevant executable(s) for coverage (opencv_5g, guided, trace, line_detection, demo) using sample media. For ONNX-related updates, run `onnx_test.exe` with target model (e.g., `model/LR.onnx img/test.jpg --size 320x320`). Update config/config.json notes if behavior depends on new settings; ensure missing files handled gracefully. Follow conventional commit style (e.g., feat(detector): ...). Include screenshots/clips for vision results when possible; document configuration/model path changes in PR notes.