C++20, Allman braces, 4-space indent. Naming: camelCase for functions/variables, PascalCase for types. Favor const references; judicious auto. Use spdlog for runtime logging (avoid std::cout). Keep behavior configuration-driven via config/config.json, avoid hardcoding. Prefer small helper functions in related headers. Handle missing files gracefully; avoid blocking operations in real-time loops. Models kept under model/, large assets not committed.